POSTGRES_PASSWORD=fastapi_traefik
TAS_USER=CHANGE_ME
TAS_SECRET=CHANGE_ME
JWT_SECRET=test_secret
ALG=HS256
TAS_URL=https://tas-dev.tacc.utexas.edu/api-test
ENVIRONMENT=dev
ENV=dev
DATABASE_URL=postgresql://fastapi_traefik:fastapi_traefik@localhost:5432/fastapi_traefik

# Tapis Development Authentication (for local testing only)
# Set to true to emulate Tapis Pod authentication headers
ENABLE_DEV_TAPIS_HEADERS=false

# Test user credentials (only used when ENABLE_DEV_TAPIS_HEADERS=true)
DEV_TAPIS_USERNAME=testuser
DEV_TAPIS_TENANT=tacc
DEV_TAPIS_SITE=tacc
# DEV_TAPIS_INTERNAL will be auto-generated as username.tenant.site if not set
