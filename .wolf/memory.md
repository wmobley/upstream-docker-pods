# Memory

> Chronological action log. Hooks and AI append to this file automatically.
> Old sessions are consolidated by the daemon weekly.
| 2026-07-17 | Added GET /user-roles/me self-lookup endpoint; fixed get_current_user dev-bypass to respect TAPIS_ENFORCE_AUTH_IN_DEV | app/api/v1/routes/user_roles.py, app/api/dependencies/auth.py | 141/141 backend tests pass, 5 new tests added | ~45000 |
| 2026-08-04 | Fixed note_scope enum mismatch (SAEnum defaulted to .name "CAMPAIGN"/"STATION" instead of lowercase .value used by the DB type); added values_callable | app/db/models/note.py | Not run locally — venv missing deps (geoalchemy2); needs pytest in dev container before deploy | ~8000 |
| 15:37 | Investigated pod-bundle-creation CORS APPROVEDADMIN failure; found today's commit 2c8932e baked cors_allow_origins into create_pod() on the false assumption the gate is edit-only — it also blocks creation. Presented fix plan, no code changed yet. | app/services/pods_service.py, app/api/v1/routes/pods.py | investigated | ~15k |
| 15:53 | Implemented CORS bootstrap fix for bug-002: reverted create-payload CORS baking, added _bootstrap_pod_cors() using service token to self-grant per-pod APPROVEDADMIN then PUT CORS; surfaced cors_status in bundle response. Security-reviewed first, user confirmed per-pod scope + self-approve authorization. | app/services/pods_service.py, app/api/v1/routes/pods.py, tests/test_pods_service.py | 139/139 backend tests pass | ~50k |
| 16:03 | Fixed bug-003: create_pod() now treats "already exists"/"uniqueviolation" as success like create_stack()/create_volume(), so retrying a partially-failed bundle creation (e.g. leftover snifferpostgres) no longer 502s with a raw Postgres IntegrityError. Added 2 tests. | app/services/pods_service.py, tests/test_pods_service.py | 141/141 backend tests pass | ~20k |
| 16:07 | Committed and pushed both pod fixes (CORS bootstrap + create_pod idempotency) as commit 5bf86ff to feature/contextual-notes; CI build-and-push-docker-image workflow triggered, watching in background. | app/services/pods_service.py, app/api/v1/routes/pods.py, tests/test_pods_service.py | pushed, CI running | ~10k |
| 16:13 | CI for 5bf86ff: pytest+build-and-push succeeded (image published to ghcr.io), deploy-develop job failed on unrelated Tapis/LDAP auth error (LDAPSocketOpenError fetching deploy token for upstreamdevelopapi) - not a code issue. User will handle deployment manually. | (CI only, no code changes) | image published, deploy left to user | ~5k |
| 16:31 | Merged feature/contextual-notes into main (68a0359) via isolated worktree to avoid disturbing working tree. Resolved 3 real conflicts (CI workflow trigger, pods_service.py, test_pods_service.py) - main independently had its own CORS-baking bug (716af6f) plus display_name/[upstream]-marker/TAPIS_BASE_URL additions we needed to preserve while still applying our bootstrap-based fix. Verified 146/146 tests + mypy clean before pushing to main. | app/services/pods_service.py, app/api/v1/routes/pods.py, tests/test_pods_service.py, .github/workflows/build-docker-image.yaml | pushed to main, CI running | ~40k |
| 16:33 | CI for main merge (68a0359) fully green: pytest+build-and-push+deploy-develop all succeeded, upstreamdevelopapi auto-redeployed with merged code. Prior LDAP auth error was transient. | (CI only) | deployed to develop pod | ~5k |
| 16:47 | Fixed bug-004: alembic migration a1b2c3d4e5f6 double-created note_scope enum (raw CREATE TYPE + sa.Enum in create_table), deterministically wedging pod startup in its retry-until-success loop. Removed redundant raw statement, verified end-to-end against real postgis/postgis:17-3.5 container. Pushed to feature/contextual-notes (d14182f) and cherry-picked to main (d69aaa4). | alembic/versions/a1b2c3d4e5f6_add_notes_table.py | verified against real Postgres, 141/141 tests pass, pushed both branches, CI running on main | ~35k |
| 16:48 | CI for main migration-fix commit d69aaa4: pytest+build-and-push+deploy-develop all green, upstreamdevelopapi redeployed with the fixed migration. | (CI only) | deployed to develop pod | ~3k |
| 17:03 | Fixed stale .env.sample TAPIS_BASE_URL/TAPIS_TENANT_ID (tacc.tapis.io/tacc -> portals.tapis.io/portals, confirmed correct with user) causing "invalid tenant id." on CORS bootstrap service-token fetch. Pushed feature/contextual-notes (be092ad) and cherry-picked to main (3175c50). Sample fix only - live pod env var still needs manual correction by user. | .env.sample | 141/141 tests pass, pushed both branches | ~15k |
| 17:10 | Fixed bug-005: build_bundle() ran CORS bootstrap (service-account self-grant) before grant_default_admin_permissions(), so the service account had zero access to the brand-new pod and Tapis rejected the permissions POST outright. Swapped order, added ordering regression test. Pushed feature/contextual-notes (7da4b55) and cherry-picked to main (fad065d). | app/services/pods_service.py, tests/test_pods_service.py | 142/142 (feature branch) and 147/147 (main) tests pass, CI running on main | ~25k |
| 17:15 | CI for main ordering-fix commit fad065d: pytest+build-and-push+deploy-develop all green, upstreamdevelopapi redeployed. | (CI only) | deployed to develop pod | ~3k |
| 20:45 | Made grant_default_admin_permissions() fault-tolerant: wraps each stack/volume/pod grant in _grant_or_log(), records {status:failed,error} per-grant instead of raising, so one "Not authorized" failure (which was hard-blocking the entire bundle after the recent ordering fix) no longer blocks the others or the whole bundle. Added test covering partial failure. Pushed feature/contextual-notes (b5b36ec) and cherry-picked to main (84ef8e2, verified content, 148/148 tests pass). This does not fix the underlying "why is wmobley not authorized" question - it makes the failure visible/non-blocking so the next test surfaces exactly which grant fails. | app/services/pods_service.py, tests/test_pods_service.py | pushed both branches, CI running | ~20k |
| 20:49 | CI caught a mypy regression I introduced in the previous fix (untyped grant_fn param, uninferable lambda default-arg) - not caught locally since I only ran pytest, not mypy. Fixed with Callable type annotation + removed unnecessary default-arg trick. Verified mypy+pytest locally this time before pushing. Pushed feature/contextual-notes (f169bb2) and cherry-picked to main (0f2f7a5). Logged process gap to cerebrum Do-Not-Repeat. | app/services/pods_service.py | mypy clean, 148/148 tests pass, pushed both branches, CI running | ~10k |
| 20:51 | CI for main mypy-fix commit 0f2f7a5: fully green (pytest+mypy+build-and-push+deploy-develop). upstreamdevelopapi redeployed with fault-tolerant permission granting. | (CI only) | deployed to develop pod | ~3k |
| 07:53 | Removed automated CORS/APPROVEDADMIN bootstrap entirely per user decision after bug-002 through bug-005 all failed to resolve "Not authorized" on the permissions endpoint. Cleaned up _bootstrap_pod_cors(), _fetch_service_token(), token-override plumbing, ui_origin/api_cors_config. CORS is now purely manual/out-of-band again. Pushed feature/contextual-notes (2ab8eee) and hand-edited equivalent directly onto main (7d0d701, since main has diverged with display_name content this branch lacks) rather than cherry-pick. mypy+pytest verified on both. | app/services/pods_service.py, app/api/v1/routes/pods.py, tests/test_pods_service.py | mypy clean, 139 (feature)/144 (main) tests pass, pushed both, CI running on main | ~40k |
| 07:54 | CI for main revert commit 7d0d701: fully green (pytest+mypy+build-and-push+deploy-develop). upstreamdevelopapi redeployed without CORS/APPROVEDADMIN automation. | (CI only) | deployed to develop pod | ~3k |
| 08:16 | Added TAS-allocation-based USER role elevation gated by new IS_PRIMARY_INSTANCE setting (never set on bundle pods). Fixed dormant TASClient() crash (missing credential defaulting) and missing timeout on projects_for_user(). Added fail-closed startup check for placeholder TAS creds when IS_PRIMARY_INSTANCE=true. Security-reviewed first (confirmed DB isolation between primary/bundle pods via per-bundle Postgres). New app/services/tas_service.py, mypy.ini override for app.pytas exclusion. Pushed feature/contextual-notes (3bce7f1) and main (82abe23, one real conflict in root.py resolved - combined with main own users/me endpoint imports). mypy+157 tests pass on main. | app/core/config.py, app/pytas/http.py, app/services/tas_service.py, app/api/dependencies/auth.py, app/api/v1/routes/root.py, mypy.ini, tests | mypy clean, 157/157 tests pass on main, pushed both branches, CI running | ~90k |
| 08:18 | CI for main TAS-allocation commit 82abe23: fully green (pytest+mypy+build-and-push+deploy-develop). upstreamdevelopapi redeployed with TAS elevation feature (inert there since IS_PRIMARY_INSTANCE unset). | (CI only) | deployed to develop pod | ~3k |
| 2026-08-04 | Wrote examples/upload_and_annotate_demo.py — dogfoods the new upstream-sdk NoteManager/MetadataSchemaManager against the Beaumont example CSVs (create campaign+station, upload, notes at every scope incl. measurement location, custom metadata field). Fixed two live-run bugs: CampaignsIn.allocation is required (added --allocation/UPSTREAM_ALLOCATION), and Point.coordinates is a generated oneOf wrapper (need .actual_instance, not subscriptable directly). User ran it live end-to-end successfully: campaign/station created, upload succeeded, all 4 note scopes + both location listings + metadata-schema field all worked. Noted separately: upload response's "Total measurements added to database" returned -1 despite the upload actually working — flagged as a likely unrelated backend bug, not yet investigated. | examples/upload_and_annotate_demo.py | Verified against live API by user, fully working | ~35k |
| 18:20 | Diagnosed CI mypy failure on feature/contextual-notes push (run 30937394891): examples/upload_and_annotate_demo.py import-not-found for upstream-sdk pkgs since mypy.ini exclude regex did not cover examples/. Added ^examples/ to exclude. | mypy.ini, .wolf/buglog.json | fix applied locally, not yet committed/pushed | ~15k |
| 12:19 | Fixed station detail Improper Allocation 404 by normalizing `Campaign.allocation` before CKAN org comparison; added direct dependency regression tests. | app/api/dependencies/pytas.py, tests/api/dependencies/test_pytas.py | `.venv/bin/python -m pytest -q` 167 passed; `.venv/bin/python -m mypy .` clean | ~22k |
| 13:12 | Fixed CKAN dataset-name conflict handling for station publish/upload flows. | app/services/ckan_service.py, app/services/ckan_publish.py, app/api/v1/routes/campaigns/*.py, app/api/v1/schemas/campaign.py, tests | Added suggested-name conflict errors, explicit `patch_existing_ckan_dataset`, and `ckan_dataset_name`; `.venv/bin/python -m pytest -q` 170 passed; `.venv/bin/python -m mypy .` clean | ~30k |
| 13:46 | Resolved PR #8 against current main in an isolated worktree; combined CKAN dataset conflict options with main's project-param/source-url behavior and fixed merge-only test breakages. | app/services/ckan_publish.py, app/services/ckan_service.py, tests/api/dependencies/test_pytas.py, tests/test_campaign_station_sensor_measurement_routes.py | `.venv/bin/python -m pytest` 186 passed; `.venv/bin/python -m mypy .` clean | ~35k |
| 2026-08-14 | Implemented upload ingestion optimization: chunked uploads share a client upload_session_id (Form fields upload_session_id/finalize_upload/chunk_index/total_chunks, defaults keep legacy behavior); measurements inserted per chunk, post-processing (sensor stats, geometry) runs once after server-verified session completeness; CKAN deferred via FastAPI BackgroundTasks (run_ckan_sync_upload opens own SessionLocal, reloads by id, token in-memory only, never logged); typed UploadFileCsvResponse keeps legacy keys + audit/post_processing/ckan_sync; migration 4e3905ac2ce5 merges 3 heads + adds nullable session/audit columns + partial index. UI uses a fetch wrapper in useUploadData.ts since openapi-generator-cli unavailable (deviation recorded). Verified migration up/down against real Postgres 18, 188/188 tests, mypy + black + flake8 clean, UI tsc/eslint clean. | app/api/v1/routes/upload_file/upload_csv.py, app/utils/upload_csv.py, app/api/v1/schemas/upload.py, app/db/models/upload_file_event.py, alembic/versions/4e3905ac2ce5*.py, tests/test_upload_csv_ingestion.py, upstream-ui/src/hooks/station/useUploadData.ts, upstream-ui/.../UploadDataModal.tsx | implemented, verified | ~50k
